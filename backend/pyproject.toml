[project]
name = "maziar-compile-server"
version = "0.1.0"
description = "Local LaTeX compile server for the Maziar browser extension"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
